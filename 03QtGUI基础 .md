

回顾：

1.C++中，未显式定义函数时，成员函数默认是**private**的

2.C++构造函数有哪几种？



### 一、绝对布局的控件

#### 1.创建步骤

```
步骤：
1.new创建控件  
2.setGeometry设置位置大小  
3.setStyleSheet设置样式
```

#### 2.正则表达式格式

```
QRegExp regExp("[0-9a-zA-Z_]+$");
```

拆解一下：

`[0-9a-zA-Z_]`→ 方括号表示"允许的字符集合"
`0-9 `= 数字0到9
`a-z` = 小写字母
`A-Z `= 大写字母
`_ `= 下划线

`+` → 表示"至少1个字符"
`$` → 表示"到字符串结尾"
常用正则：

`[0-9]+` → 只能输入数字
`[a-zA-Z]+` → 只能输入字母
`.{6,12}` → 任意字符，6到12位
`1[3-9]\d{9}` → 手机号格式



#### 3.信号与槽

```c++
// 基本格式
connect(发送者, SIGNAL(信号名()), 接收者, SLOT(槽函数名()));

/*
发送者：this->loginBtn（登录按钮）
信号：clicked()（被点击）
接收者：this（当前窗口对象）
槽函数：doLogin()（执行登录）
*/
connect(loginBtn, SIGNAL(clicked()), this, SLOT(doLogin()));
```

意思是：当 `loginBtn` 发出 `clicked()` 信号时，调用 this 对象的 `doLogin() `槽函数。

信号 = 事件发生了（按钮被点击）
槽 = 响应这个事件的函数

人话：当登录按钮被点击时，调用 doLogin 函数。



注意事项：

槽函数必须在头文件里声明在 `public slots:` 或 `private slots:` 下面
信号和槽的参数类型要匹配



#### 4.密文模式

Qt自带的！

```c++
this->pwdEdit->setEchoMode(QLineEdit::Password);
```

Qt 提供了4种显示模式：

`QLineEdit::Normal `→ 正常显示（默认）
`QLineEdit::Password` → 显示圆点 ●●●●●●
`QLineEdit::NoEcho` → 什么都不显示（连圆点都没有）
`QLineEdit::PasswordEchoOnEdit` → 输入时显示字符，失去焦点变圆点



#### 5.去掉前后空格trimmed（）

在获取输入框内容时，为防止用户输入" admin " ,需要用到trimmed（）

```c++
// 获取输入框内容，trimmed()去掉前后空格
    QString name = this->userEdit->text().trimmed();
    QString pwd = this->pwdEdit->text().trimmed();
```



#### 6.弹窗提示

```c++
// 样式
QMessageBox::information(this, "标题", "内容");  // 普通提示
QMessageBox::warning(this, "标题", "内容");      // 警告提示
```

warning

![image-20260102165619005](C:\Users\28293\AppData\Roaming\Typora\typora-user-images\image-20260102165619005.png)

information

![image-20260102165648542](C:\Users\28293\AppData\Roaming\Typora\typora-user-images\image-20260102165648542.png)



#### 7.调试输出qDebug()

```c++
qDebug() << "用户名:" << name;
```

在 Qt Creator 底部的"应用程序输出"窗口可以看到，方便调试。



### 8.鼠标样式设置setCursor（）

```c++
this->loginBtn->setCursor(Qt::PointingHandCursor);
```

就是当鼠标移到按钮上时，鼠标指针从普通箭头 ↖ 变成小手 👆

这是一个用户体验细节，告诉用户"这个东西可以点击"。你平时浏览网页，鼠标移到链接上会变小手，就是这个效果。

Qt 常用的鼠标样式：

`Qt::ArrowCursor` → 普通箭头（默认）
`Qt::PointingHandCursor` → 小手（可点击）
`Qt::WaitCursor` → 等待圆圈
`Qt::IBeamCursor` → 文本输入光标 |
`Qt::CrossCursor` → 十字准星

### QSS常用样式

Qt StyleSheet（QSS）的常用样式属性，和 CSS 很像：

一、基础样式属性
`color` - 文字颜色，如 `color: #333333;` 或 `color: white;`
`background-color` - 背景颜色，如 `background-color: #f0f0f0;`
`background-image` - 背景图片，如 `background-image: url(:/images/bg.png);`
`font-size` - 字体大小，如 `font-size: 16px;`
`font-family` - 字体，如 `font-family: "Microsoft YaHei";`
`font-weight` - 字体粗细，如 `font-weight: bold;`
二、边框相关
`border` - 边框，如 `border: 1px solid #ccc;`  可以设置的属性分别（按顺序）：border-width, border-style 和 border-color，可以只设置一个值，其他值会设置成对应属性的初始值
`border-radius` - 圆角，如 `border-radius: 8px;`（数值越大越圆）
`border-top/bottom/left/right` - 单边边框
`border: none;` - 去掉边框
三、内边距和尺寸
`padding` - 内边距，如 `padding: 10px 20px;`（上下10，左右20）
`min-width` / `min-height` - 最小宽高
`max-width` / `max-heigh` - 最大宽高
四、伪状态（控件在不同状态下的样式，让控件更有交互感）
`:hover` - 鼠标悬停时的样式
`:pressed` - 按下时的样式
`:disabled` - 禁用时的样式
`:focus` - 获得焦点时的样式
`:checked` - 选中状态（用于复选框、单选框）







### 补充&回顾

#### 1.内边距padding是什么？

```
┌─────────────────────────┐  ← 边框
│      ↑ padding-top      │
│   ┌───────────────┐     │
│ ← │    文字内容    │ →   │  ← padding-left/right
│   └───────────────┘     │
│      ↓ padding-bottom   │
└─────────────────────────┘

```

`padding: 10px;` → 四边都是10像素
`padding: 10px 20px; `→ 上下10像素，左右20像素
`padding: 5px 10px 15px 20px; `→ 上5、右10、下15、左20（顺时针）

没有 padding 的输入框，文字会紧贴边框，很丑；加了 padding，文字和边框有间距，看起来舒服。